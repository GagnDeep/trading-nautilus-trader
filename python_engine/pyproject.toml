[project]
name = "nautilus_engine"
version = "0.1.0"
description = "NautilusTrader Orchestration Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "nautilus_trader",
    "fastapi",
    "uvicorn",
    "redis",
    "pandas",
    "numpy",
    "msgpack",
    "websockets",
    "prisma",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "black",
    "isort",
]
